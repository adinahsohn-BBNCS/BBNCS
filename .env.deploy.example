# Copy to .env.deploy and fill in (never commit .env.deploy)
# BBNCS / bbncs.com ONLY — do not use Fly LRE credentials here

SFTP_HOST=bbncs.com
SFTP_PORT=22
SFTP_USER=your_cpanel_username
SFTP_PASSWORD=your_cpanel_password
SFTP_REMOTE_DIR=/home/your_cpanel_username/public_html
